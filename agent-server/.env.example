# Required — get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional — register free at sam.gov to avoid rate limits
# Without this key the API still works, just at lower rate limits
SAM_API_KEY=

# Optional — defaults to 3001
PORT=3001
